import type { PartialConfigType } from 'plugin-runtime';

const conf: PartialConfigType = {
  compiler: 'tsc',
  prefixCls: 'sky',
  modifyVars: {},
  importOnDemand: [],
  proxy: [],
};

export default conf;
